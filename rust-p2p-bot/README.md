# P2P Trading Bot для Rust скинов

Автоматический бот для торговли скинами Rust между площадками TradeIt.gg, DMarket и Loot.Farm.

## Возможности

- 🔄 Автоматический поиск выгодных сделок (лучший спред)
- 💰 Торговля на TradeIt.gg, DMarket, Loot.Farm
- 🔐 Автоподтверждение трейдов через Steam Guard
- 📱 Управление через Telegram бот с inline кнопками
- 📊 Статистика и уведомления о сделках

## Установка

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Установка Playwright браузеров

```bash
playwright install chromium
```

### 3. Настройка конфигурации

Отредактируйте `config/settings.json`:
- API ключи (Pulse, DMarket, Steam, Telegram)
- Параметры торговли (мин/макс цена, мин. спред)

### 4. Настройка Steam Guard

Скопируйте данные из вашего maFile в `config/mafile.json`:
- shared_secret
- identity_secret
- device_id
- steam_id

## Запуск

### Полный запуск (с Telegram ботом)
```bash
python main.py
```

### Только тест подключений
```bash
python main.py --test-connections
```

### Тестовая сделка (без реального исполнения)
```bash
python main.py --test-trade
```

### Без Telegram бота
```bash
python main.py --no-telegram
```

### Тестовый режим (без реальных сделок)
```bash
python main.py --test
```

## Telegram бот

После запуска отправьте `/start` боту @RustBuda_bot

### Доступные функции:
- 💰 Баланс - показать балансы на всех площадках
- ▶️/⏹ Старт/Стоп - управление торговлей
- 📊 Статистика - количество сделок и профит
- ⚙️ Настройки - текущие параметры
- 🧪 Тест сделки - найти лучшую сделку без исполнения
- 🎮 Steam - информация об аккаунте

## Структура проекта

```
rust-p2p-bot/
├── main.py              # Точка входа
├── config/
│   ├── settings.json    # Настройки
│   └── mafile.json      # Steam Guard данные
├── src/
│   ├── steam_guard.py   # Автоподтверждение трейдов
│   ├── pulse_api.py     # Анализ цен
│   ├── dmarket_api.py   # DMarket API
│   ├── lootfarm.py      # Loot.Farm автоматизация
│   ├── tradeit.py       # TradeIt.gg автоматизация
│   ├── trading_logic.py # Торговый движок
│   └── telegram_bot.py  # Telegram интерфейс
└── logs/                # Логи
```

## Безопасность

⚠️ **ВАЖНО:**
- Не публикуйте файлы с API ключами
- Храните mafile.json в безопасном месте
- Используйте отдельный Steam аккаунт для торговли

## Текущие балансы

- TradeIt.gg: ~$28
- DMarket: ~$1.10
- Loot.Farm: $0

## Лицензия

Только для личного использования.
