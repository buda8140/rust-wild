# 📊 ПОЛНАЯ АРХИТЕКТУРА АЛГОРИТМА ПОИСКА СПРЕДА

**Правильный способ:** Поиск **ЛУЧШЕГО СПРЕДА** (разницы в цене), а не просто дешевого и дорогого

---

## 🎯 ЧТО ТАКОЕ СПРЕД И ПОЧЕМУ ОН ВАЖЕН

### Определение:
```
Спред = (Цена_Продажи / Цена_Покупки - 1) × 100%

Пример:
- Купили на DMarket: $0.80
- Продали на Loot.Farm: $1.10
- Спред = (1.10 / 0.80 - 1) × 100% = 37.5% прибыль! 
```

### Почему это работает:
1. ✅ Покупаем где **ДЕШЕВЕ**
2. ✅ Продаем где **ДОРОЖЕ**
3. ✅ Получаем **ПРИБЫЛЬ**
4. ✅ **ПОВТОРЯЕМ** бесконечно

---

## 📈 ПРАВИЛЬНЫЙ АЛГОРИТМ

```
ЦИКЛ КАЖДЫЕ 10 СЕКУНД:
│
├─ ШАГ 1: ОПРЕДЕЛИТЬ СТАРТОВУЮ ПЛОЩАДКУ
│  │
│  ├─ Где МАКСИМУМ денег?
│  │  ├─ TradeIt.gg: $24.6 ← ВЫБИРАЕМ!
│  │  ├─ DMarket: $1.10
│  │  └─ Loot.Farm: $0
│  │
│  └─ Стартовая площадка = TradeIt.gg
│
├─ ШАГ 2: ПРОВЕРИТЬ ВСЕ КОМБИНАЦИИ
│  │
│  ├─ Комбо 1: TradeIt.gg (покупка) → Loot.Farm (продажа)
│  │  ├─ Запрос Pulse API CompareTables
│  │  ├─ Получить список предметов со спредом ≥ 10%
│  │  └─ Лучший спред: "AK-47 Victoria" +35% ✅
│  │
│  ├─ Комбо 2: TradeIt.gg (покупка) → DMarket (продажа)
│  │  ├─ Запрос Pulse API CompareTables
│  │  ├─ Получить список предметов со спредом ≥ 10%
│  │  └─ Лучший спред: "Abyss AR" +28% 
│  │
│  └─ Выбрать МАКСИМАЛЬНЫЙ спред = "AK-47 Victoria" +35%
│
├─ ШАГ 3: ПОЛУЧИТЬ ДЕТАЛИ ПРЕДМЕТА
│  │
│  ├─ Проверить:
│  │  ├─ ✅ Цена не в overstockInfo
│  │  ├─ ✅ Статус не "unstable"  
│  │  ├─ ✅ Есть в наличии у ботов
│  │  ├─ ✅ Цена между $0.50 - $3.00
│  │  └─ ✅ Количество продаж за неделю ≥ 15
│  │
│  └─ Статус: ✅ ПРЕДМЕТ ВАЛИДЕН
│
├─ ШАГ 4: ПОКУПКА НА ДЕШЕВОЙ ПЛОЩАДКЕ (TradeIt.gg)
│  │
│  ├─ Предмет: "AK-47 Victoria"
│  ├─ Цена: $68.80
│  ├─ Действие: 
│  │  ├─ Открыть https://tradeit.gg/ru/rust/trade
│  │  ├─ Поиск "AK-47 Victoria"
│  │  ├─ Добавить в корзину
│  │  ├─ Оформить покупку
│  │  └─ Получить в Steam инвентарь
│  │
│  └─ Статус: ⏳ Ожидание в Steam инвентаре (5-30 сек)
│
├─ ШАГ 5: ПОЛУЧЕНИЕ В STEAM И ОБНОВЛЕНИЕ
│  │
│  ├─ Проверяем Steam инвентарь Rust каждые 2 сек
│  ├─ Когда предмет появился:
│  │  ├─ Обновляем инвентарь на Loot.Farm
│  │  └─ Ищем где обменять по лучшей цене
│  │
│  └─ Статус: ✅ ГОТОВО К ОБМЕНУ
│
├─ ШАГ 6: ПОИСК ВЫГОДНОГО ОБМЕНА (Loot.Farm)
│  │
│  ├─ JSON API: https://loot.farm/fullpriceRUST.json
│  ├─ Поиск: Какие предметы дороже чем на DMarket?
│  ├─ Результат: 
│  │  ├─ Предмет A: Loot.Farm $72 vs DMarket $71 (+1%) ✗
│  │  ├─ Предмет B: Loot.Farm $75 vs DMarket $73 (+2.7%) ✓
│  │  ├─ Предмет C: Loot.Farm $79 vs DMarket $74 (+6.7%) ✓
│  │  └─ Лучший вариант: Предмет C (+6.7%) или остальные (+2.7%)
│  │
│  ├─ Выбираем Предмет C
│  └─ Статус: ✅ ОБМЕН НАЙДЕН
│
├─ ШАГ 7: СОЗДАНИЕ ТРЕЙД-ОФФЕРА (Loot.Farm → Steam)
│  │
│  ├─ Мы отправляем:  "AK-47 Victoria" ($68.80)
│  ├─ Получаем:       "Предмет C" (~$74-79)
│  ├─ Действие:
│  │  ├─ API Loot.Farm: Создать trade offer
│  │  ├─ Лот.Farm создает трейд Steam
│  │  └─ Нам приходит уведомление Steam
│  │
│  └─ Статус: ⏳ Ожидание подтверждения
│
├─ ШАГ 8: АВТОПОДТВЕРЖДЕНИЕ ТРЕЙДА (steam-master)
│  │
│  ├─ Получаем уведомление о трейде
│  ├─ Используем steam-master (не SDA.exe!):
│  │  ├─ Генерируем 2FA код
│  │  ├─ Получаем список подтверждений
│  │  ├─ Находим наш трейд
│  │  └─ Подтверждаем АВТОМАТИЧЕСКИ (БЕЗ ЗАДЕРЖЕК!)
│  │
│  └─ Статус: ✅ ТРЕЙД ПОДТВЕРЖДЕН!
│
├─ ШАГ 9: ПОЛУЧЕНИЕ ПРЕДМЕТА И ПРОДАЖА
│  │
│  ├─ Предмет C теперь в нашем Steam инвентаре
│  ├─ Обновляем инвентарь на DMarket
│  ├─ Выставляем на продажу:
│  │  ├─ Предмет C
│  │  ├─ Цена: $74 (лучшая цена на рынке)
│  │  └─ DMarket берет комиссию → Мы получаем ~$71
│  │
│  └─ Статус: ✅ ПРОДАНО!
│
└─ ШАГ 10: ПОДСЧЕТ ПРИБЫЛИ И ПОВТОР
   │
   ├─ Потратили: $68.80
   ├─ Получили:  $71.00
   ├─ Прибыль:   $2.20 (+3.2%)
   ├─ Новый баланс TradeIt: $24.6 - $68.80 + $71 = $26.8 (+2.2%)
   │
   ├─ Повторяем ШАГ 1-10 с новым балансом
   └─ ЦИКЛ ПОВТОРЯЕТСЯ! 🔄
```

---

## 🔢 МАТЕМАТИКА СПРЕДА

### Пример из видео (канал Труд):

**ПЕРВЫЙ КРУГ:**
```
1. Купил на DMarket: Power Station Door = $0.84
2. Залил на Loot.Farm: получил = $1.26
3. Спред = (1.26 / 0.84) = 1.5 = +50%! ✅

Прибыль первого круга: $1.26 - $0.84 = +$0.42 (+50%)
Банк стал: $0.30 → $0.30 + $0.42 = $0.72
```

**ВТОРОЙ КРУГ:**
```
1. Купил на DMarket: предметы на $4.19
2. Залил на Loot.Farm: получил $7.09
3. Спред = (7.09 / 4.19) = 1.69 = +69%! ✅

Прибыль второго круга: $7.09 - $4.19 = +$2.90 (+69%)
Банк стал: $0.72 → $0.72 + $2.90 = $3.62
```

**ТРЕТИЙ КРУГ:**
```
Спред +33% = $3.62 + $1.20 = $4.82
Спред +25% = $4.82 + $1.20 = $6.02
Спред +20% = $6.02 + $1.20 = $7.22

За 1 ЧАС: $0.30 → $7.22 = +2300% ПРИБЫЛЬ!!! 🚀
```

---

## ⚙️ ФИЛЬТРЫ ДЛЯ ВЫБОРА ПРЕДМЕТА

### КРИТЕРИИ ГОДНОСТИ:

```python
def is_item_valid(item_data, pulse_response):
    # 1. Спред ≥ 10%
    spread = (item_data['sell_price'] / item_data['buy_price'] - 1) * 100
    if spread < 10:
        return False, f"Спред только {spread}%"
    
    # 2. Не в overstockInfo
    if pulse_response.get('overstockInfo', {}).get('currentCount', 0) > 0:
        return False, "Overstock - отказ ботом"
    
    # 3. Не unstable (был большой скачок цены)
    if 'unstable' in pulse_response.get('status', ''):
        return False, "Статус unstable"
    
    # 4. Есть в наличии
    if pulse_response.get('totalSellOffersCount', 0) == 0:
        return False, "Нет в наличии"
    
    # 5. Цена в диапазоне $0.50 - $3.00
    price = item_data['buy_price']
    if price < 0.50 or price > 3.00:
        return False, f"Цена {price} вне диапазона"
    
    # 6. Популярность (продажи за неделю ≥ 15)
    if pulse_response.get('sales7Day', 0) < 15:
        return False, f"Малопопулярен: {sales7Day} продаж/неделю"
    
    return True, "✅ ВАЛИДЕН"
```

---

## 📍 ИСПОЛЬЗУЕМЫЕ ИСТОЧНИКИ ДАННЫХ

### 1. **Pulse API CompareTables** - Основной
```
GET https://api-pulse.tradeon.space/public-api/item/compare-tables
- Сравнение цен между площадками
- Стоимость: 2 токена за запрос
- Частота: Каждые 10 сек (макс. 6 запросов, но используем 3)
```

### 2. **Loot.Farm JSON API** - Вспомогательный (БЕСПЛАТНО!)
```
GET https://loot.farm/fullpriceRUST.json
- Полный список цен ботов
- Обновляется каждую минуту
- БЕЗ токенов! ✅
```

### 3. **DMarket Trading API** - Покупка/Продажа
```
POST https://api.dmarket.com/v1/...
- Управление инвентарем
- Создание офферов
- Подтверждение трейдов
```

### 4. **TradeIt.gg Веб** - Покупка (Selenium)
```
Selenium WebDriver:
- Поиск предметов
- Добавление в корзину
- Оформление покупки
```

---

## 🚀 РЕАЛИЗАЦИЯ В КОДЕ

### Главный файл: `src/trade_engine.py`

```python
class TradeEngine:
    def __init__(self):
        self.pulse = PulseAPI(api_key)
        self.dmarket = DMarketAPI()
        self.tradeit = TradeItBot()
        self.lootfarm = LootFarmAPI()
        self.steam = SteamAuthenticator(mafile_path)
    
    async def find_best_spread(self):
        """Найти ЛУЧШИЙ спред из всех комбинаций"""
        
        best_deal = None
        max_spread = 0
        
        # Все возможные комбинации
        combinations = [
            ('TradeIt', 'LootFarm'),
            ('TradeIt', 'DMarket'),
            ('DMarket', 'LootFarm'),
            ('DMarket', 'TradeIt'),
            ('LootFarm', 'TradeIt'),
            ('LootFarm', 'DMarket'),
        ]
        
        for source, target in combinations:
            # CompareTables Pulse API
            items = self.pulse.compare_tables(source, target)
            
            for item in items:
                spread = self.calculate_spread(item)
                
                if spread > max_spread and self.is_item_valid(item):
                    max_spread = spread
                    best_deal = {
                        'name': item['name'],
                        'source': source,
                        'target': target,
                        'buy_price': item['source_price'],
                        'sell_price': item['target_price'],
                        'spread': spread
                    }
        
        return best_deal
    
    async def execute_trade(self, deal):
        """Выполнить полный цикл трейда"""
        
        logger.info(f"🚀 Начинаем трейд: {deal['name']}")
        logger.info(f"   Покупаем на {deal['source']}: ${deal['buy_price']}")
        logger.info(f"   Продаем на {deal['target']}: ${deal['sell_price']}")
        logger.info(f"   Спред: {deal['spread']}%")
        
        # 1. КУПИТЬ
        if deal['source'] == 'TradeIt':
            await self.tradeit.buy_item(deal['name'], deal['buy_price'])
        elif deal['source'] == 'DMarket':
            await self.dmarket.buy_item(deal['name'], deal['buy_price'])
        
        # 2. ПОЛУЧИТЬ В STEAM (ждем 5-30 сек)
        await self.wait_for_inventory(deal['name'])
        
        # 3. ОБМЕНЯТЬ/ПРОДАТЬ
        if deal['target'] == 'LootFarm':
            await self.lootfarm.create_trade(give=deal['name'], get=best_match)
        elif deal['target'] == 'DMarket':
            await self.dmarket.create_offer(deal['name'], deal['sell_price'])
        
        # 4. ПОДТВЕРДИТЬ (steam-master)
        await self.steam.auto_confirm_trade()
        
        # 5. ПОЛУЧИТЬ ДЕНЬГИ
        profit = deal['sell_price'] - deal['buy_price']
        logger.success(f"✅ Прибыль: ${profit} (+{deal['spread']}%)")
        
        # Повтор цикла
        return profit
```

---

## 📊 ОПТИМИЗАЦИЯ ТОКЕНОВ

### Текущие затраты:
- CompareTables: 2 токена × 6 комбинаций = 12 токенов/цикл
- Цикл каждые 10 сек = 8640 токенов/день = $8.64/день ❌ МНОГО!

### Оптимизированные затраты:
- CompareTables: 2 токена × 3 комбинации = 6 токенов/цикл
- + Loot.Farm JSON (БЕСПЛАТНО) = 0 токенов
- Цикл каждые 30 сек = 2880 токенов/день = $2.88/день ✅ НОРМАЛЬНО!

**Решение:**
1. Использовать только 3 выгодные комбинации
2. Использовать Loot.Farm JSON API вместо CompareTables где возможно
3. Увеличить интервал между запросами (30 сек вместо 10)

